#spring.application.name=workbook-zero

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/umc8th
    username: root
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialects
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true